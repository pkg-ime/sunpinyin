sunpinyin for Debian
--------------------

The source package is generated from git repository on commit 
dae49d1c153b37b1b90eb3d4ffb4067fe6a6661b ON Aug 18 2010 with command:

git archive --format=tar --prefix=sunpinyin-2.0.2/ \
dae49d1c153b37b1b90eb3d4ffb4067fe6a6661b `ls|grep -v debian|grep -v wrapper`\
|bzip2 -9  >../sunpinyin-2.0.2.tar.bz2

The wrapper directory contains ibus, macos, scim, xim and standalone frontend 
of sunpinyin, which may be built to standalone packages.
